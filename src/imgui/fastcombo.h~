#pragma once

#include <string>
#include <vector>

#include <imgui.h>

namespace MyGui {

	class FastCombo
	{
	public:
		FastCombo(const std::string& label, std::vector<std::string>& values)
			{
				
			}
	};
	
}
